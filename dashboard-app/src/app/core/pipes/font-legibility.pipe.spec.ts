import { FontLegibilityPipe } from './font-legibility.pipe';

describe('FontLegibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new FontLegibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
